<h1>About</h1>

{{#each model as |bookmark|}}
  {{#link-to 'bookmarks.edit' bookmark}}Edit Bookmarks{{/link-to}}
  {{#if bookmark.public}}
    {{bookmark-listing bookmark=bookmark}}
  {{else}}
    <article>Not public bookmark</article>
  {{/if}}
{{/each}}
